#User function Template for python3

class Solution:
	def binaryToDecimal(self, b):
		# Code here
		b = b[::-1]
		totSum = 0
		
		n = len(b)
		for i in range(n):
		    if b[i] == '1':
		        totSum += 2 ** i
		
		return totSum

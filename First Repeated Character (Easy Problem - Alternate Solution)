#User function Template for python3

class Solution:
    def firstRepChar(self, s):
        # code here
        seen = set()
        
        for ch in s:
            if ch in seen:
                return ch
            seen.add(ch)
            
        return -1

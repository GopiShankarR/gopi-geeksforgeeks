class Solution:
    def largest(self, arr):
        # code here
        n = len(arr)
        mx = arr[0]
        for i in range(1, n):
            if arr[i] > mx:
                mx = arr[i];
                
        return mx

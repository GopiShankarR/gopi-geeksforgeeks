class Solution:
    def maxSum(self, arr):
        # code here
        n = len(arr)
        mx = -1
        for i in range(n - 1):
            mx = max(mx, (arr[i] + arr[i + 1]))
        
        return mx

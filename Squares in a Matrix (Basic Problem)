#User function Template for python3

class Solution:
    def squaresInMatrix(self, m, n):
        # code here 
        if n < m:
            temp = m
            m = n
            n = temp
        return int(m * (m + 1) * (2 * m + 1) / 6 + (n - m) * m * (m + 1) / 2)

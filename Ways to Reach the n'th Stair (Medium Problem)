class Solution:
    def countWays(self, n):
        # code here
        
        if n == 1:
            return 1
        
        L = [1] * n
        L[0] = 1
        L[1] = L[0] + L[0]
        
        for i in range(2, n):
            L[i] = L[i - 2] + L[i - 1]
        
        return L[n - 1]

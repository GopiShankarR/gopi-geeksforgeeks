#User function Template for python3

class Solution:
    def fact(self, n):
        if n == 0:
            return 1
        
        return self.fact(n - 1) * n
    def nPr(self, n, r):
        # code here
        return int(self.fact(n) / self.fact(n - r))
